# Local Worker secrets for `wrangler dev`. Copy to .dev.vars (gitignored).
# When ACCESS_PIN is unset, the Worker allows all requests (fine for local dev).
# In production set it with: wrangler secret put ACCESS_PIN
ACCESS_PIN=dev-pin-change-me
