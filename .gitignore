# Local env / credentials
.env
.env.local
.env.*.local
.dev.vars
*_PRIVATE.md
*credentials*

# Personal health data: generated import SQL + any exported episode data.
# The CSV lives in the Obsidian vault; the DB holds the data. Never commit either.
tmp/
*.import.sql
Migraines_AllEpisodes.csv

*.local.mjs

# Agent context and private notes stay local.
CLAUDE.md
PUBLISH_READINESS.md

# Build artifacts
node_modules/
dist/
.wrangler/
.cache/

# Cloudflare generated types
worker-configuration.d.ts

# OS / editor / local tooling
.DS_Store
.vscode/
.idea/
.claude/
*.swp
tsconfig.tsbuildinfo
